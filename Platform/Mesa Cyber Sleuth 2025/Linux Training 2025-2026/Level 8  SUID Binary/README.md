SUID binary: Level 8
80
There is a small SUID binary installed at /usr/local/bin/print_flag_suid. If you can execute it with the privileges it provides, it prints the flag. Attempt to run it.

Hint
×
/usr/local/bin/print_flag_suid
